@model Gravitas.Platform.Web.ViewModel.CentralLabolatorySamples.IdleVm
@Html.HiddenFor(m => m.NodeId, new { @id = "nodeId" })

@Html.Action("GetLabLink", "Helper", new { nodeId = Model.NodeId })

<div class="d-flex align-items-center icon-container">
    <div class="flex-shrink-1"><i class="fas fa-broadcast-tower"></i></div>
    <div class="p-3 w-100"><h3>Очікування картки водія</h3></div>
</div>
