@using Gravitas.Model.DomainValue
@model Gravitas.Platform.Web.ViewModel.UnloadPointType1Vms.GetTareValue
@Html.HiddenFor(m => m.NodeId, new { id = "nodeId" })

@{
    Html.RenderAction("GetTareValue", "Helper", new { nodeId = Model.NodeId, returnRoutineStateId = OpRoutine.UnloadPointType1.State.Idle });
}
